#include <stdio.h>
/**
 * Main: prints the contents of the program
 * Return: produces 0 if program runs successfully
 */
int main(void)
	{ 	printf("2*3\n");
		printf("I am progressing in c programming\n");
		printf("It's only a matter of small time before I turn pro!\n");
		return (0);
	}
